# pnpm configuration
# Enable automatic installation of peer dependencies
auto-install-peers=true

# Strict peer dependencies - warns if peer deps are not met
strict-peer-dependencies=false

# Don't create a lockfile in nested packages
# Only the root pnpm-lock.yaml is used
shared-workspace-lockfile=true

# Preserve symlinks when resolving
prefer-workspace-packages=true

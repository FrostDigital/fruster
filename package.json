{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "lerna exec -- npm run test",
    "audit:high": "lerna exec -- npm audit --audit-level=high"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/FrostDigital/fruster.git"
  }
}
